let w= parseInt( prompt("enter any number for width"));
let l=parseInt( prompt("enter any number for length"));

if(w==l){
    let result=w*l
    console.log("area" + result)
}
else{
    let result= (w+l)*2;
   console.log ("Environment"+result)
}